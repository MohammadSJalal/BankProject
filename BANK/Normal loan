public class NormalLoan extends BaseLoan {
    public NormalLoan(Customer c, int amount, int months) {
        super(c, Math.min(amount, 500_000_000), months);
    }

    @Override
    public int calculatePenalty() {
        return (int) (getRemaining() * 0.06); // 6 درصد
    }
}
